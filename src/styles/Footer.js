import styled from 'styled-components';

export const FooterWrapper = styled.div`
  display: flex;
  justify-content: center;

  svg {
    font-size: 28px;
    margin-right: 0.7rem;
    cursor: pointer;
  }
`;
